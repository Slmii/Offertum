# Build output
dist
.output
.nitro
.tanstack

# TanStack Router auto-generated route tree (regenerated on dev/build, and in CI via
# the "Generate route tree" workflow step: `pnpm --filter @offertum/web routes:generate`)
src/routeTree.gen.ts
