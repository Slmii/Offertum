// Canonical definition lives in @offertum/shared (the web app's date formatters pin to
// the same zone). Re-exported here so existing `@/lib/time/business-time-zone` imports
// keep working.
export { BUSINESS_TIME_ZONE } from '@offertum/shared';
